package de.farmpulse.backend.domain;

/** Dringlichkeit einer {@link MailboxMessage}, siehe MockDashboard/Postfach.html ("hoch"/"mittel"/"niedrig"). */
public enum MailboxPriority {
    HIGH,
    MEDIUM,
    LOW
}
